#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

void test1() {
    // 【时间复杂度阶数说明】
    // O(1): 常数阶，执行时间不随数据量变化。
    // O(n): 线性阶，执行时间与数据量 n 成正比。
    // O(n^2): 平方阶，常见于双重循环。
    // O(n^3): 立方阶，常见于三重循环。

    // --- 示例：O(n^3) 结构 ---
    // 下面的三重循环如果 i, j, k 都运行到 n，则复杂度为 O(n^3)
    for (int i = 0; i < 10; i++) {
        for (int j = 0; j < 10; j++) {
            for (int k = 0; k < 10; k++) {
                // 场景：在三维空间中查找具有某个特征的值，或处理三层嵌套逻辑。
            }
        }
    }

    // --- 性能量化对比 (假设数据量 n = 10万) ---
    // 当 n = 100,000 时：
    // 1. O(1)   -> 执行 1 次（瞬时完成）
    // 2. O(n)   -> 执行 100,000 次（约 0.1 毫秒）
    // 3. O(n^2) -> 执行 10,000,000,000 次（100 亿次，普通 CPU 约需数秒到十秒）
    // 4. O(n^3) -> 执行 1,000,000,000,000,000 次（1000 万亿次，可能需要跑几天甚至几月）

    // 【优化建议】
    // 1. 尽量减少不必要的循环嵌套，避免“套娃”式代码。
    // 2. 空间换时间：使用哈希表（Hash Table）。
    //    只要设计合理的 Key，查找效率可以从 O(n) 降低到 O(1)，从而消除多余的循环。

    // --- 工业级代码的三大支柱 ---
    // 1. 业务功能：代码首先要能跑通，满足客户的功能需求。
    // 2. 可读性（代码质量）：代码是写给人看的，方便后期维护和团队协作。
    // 3. 性能（用户体验）：用户虽然不懂复杂度，但能感觉到界面卡不卡、加载快不快。
    //    性能差的代码在 Code Review 或上线测试阶段通常会被要求重构。

    // --- 典型场景对比 ---
    for (int i = 0; i < 10; i++) {
        // 场景 A (差): 在循环里调用 O(n) 的函数
        // linkedlist_get_by_idx(list, n); 
        // 整个循环的总复杂度会退化成 O(n^2)

        // 场景 B (优): 在循环里调用 O(1) 的查找
        // hashmap_get(map, "key");
        // 整个循环的总复杂度保持在 O(n)
    }
}

int main(void) {
    // 调用测试函数
    test1();

    return 0;
}